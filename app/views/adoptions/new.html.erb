<h1> Adoption Form Page </h1><hr/><br>

<%= form_for @adoption do |a| %>
    Select a person: <%= a.collection_select(:person_id, Person.all, :id, :first_name) %><br><br>
    Choose an animal: <%= a.collection_select(:animal_id, Animal.all, :id, :name) %><br><br>
    <%= a.submit %>
<% end %>